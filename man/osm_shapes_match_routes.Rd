% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_osm_shapes_match_routes.R
\name{osm_shapes_match_routes}
\alias{osm_shapes_match_routes}
\title{Get OSM routes that match shapes, based on geometrical match}
\usage{
osm_shapes_match_routes(
  gtfs,
  q,
  geometry = TRUE,
  gtfs_match = "route_short_name",
  osm_match = "ref"
)
}
\arguments{
\item{gtfs}{tidygtfs. GTFS feed.}

\item{q}{osmdata::opq. Overpass query for transit network}

\item{geometry}{Boolean (Default TRUE). If TRUE, returns sf object with geometry, otherwise, a simple data.frame.}

\item{gtfs_match}{String (Default route_short_name). routes.txt attribute that identifies routes. Accepted values: route_id, route_short_name, route_long_name.}

\item{osm_match}{String (Default ref). OSM attribute that identifies routes by matching with gtfs_match. Accepted values: ref, name, gtfs:route_id.}
}
\value{
A \code{data.frame} (\code{sf} if \code{geometry=TRUE}) with the following columns:
\itemize{
 \item \code{shape_id}, the \code{shape_id} attribute from \code{shapes.txt} file.
 \item \code{osm_id}, the \code{osm_id} attribute from OSM route relation.
 \item \code{distance_diff}, the difference, in meters, between GTFS shape and OSM route lengths.
 \item \code{points_diff}, the sum of the difference, in meters, between GTFS shape and OSM route start and end points.
 \item \code{route_short_name}, the \code{route_short_name} attribute from \code{routes.txt} file.
 \item \code{route_long_name}, the \code{route_long_name} attribute from \code{routes.txt} file.
 \item \code{geometry}, the geometrical data for the OSM route relation.
}
}
\description{
Get OSM routes that match shapes, based on geometrical match
}
\details{
For each route, matches its trips' shapes with OSM route relations.

The match is performed considering, for each shape, the closest OSM route considering
the start and end points and total length.
}
\examples{
\dontrun{
gtfs <- GTFShift::load_feed("gtfs.zip")

q = opq("Lisbon")  |>
  add_osm_feature(key = "route", value = c("bus", "tram")) |>
  add_osm_feature(key = "network", value = "Carris", key_exact = TRUE)

shapes_match_routes = GTFShift::osm_shapes_match_routes(gtfs, q)
}

}
